# 🎯 Phase 3 - Admin Panel Architecture

## System Flow Diagram

```
                          ┌────────────────────────────────────┐
                          │       ADMIN PANEL (React)          │
                          │     http://localhost:3000          │
                          └────────────────────────────────────┘
                                         │
                                         │ HTTP Requests
                                         │ (with JWT token)
                                         ▼
                          ┌────────────────────────────────────┐
                          │      FLASK API SERVER              │
                          │     http://localhost:5000          │
                          │                                    │
                          │  ┌──────────────────────────────┐ │
                          │  │   Authentication Layer       │ │
                          │  │   @token_required decorator  │ │
                          │  └──────────────────────────────┘ │
                          │              │                     │
                          │              ▼                     │
                          │  ┌──────────────────────────────┐ │
                          │  │     API Route Handlers       │ │
                          │  │  • /api/auth/login           │ │
                          │  │  • /api/reports              │ │
                          │  │  • /api/whitelist            │ │
                          │  │  • /api/audit                │ │
                          │  │  • /api/stats                │ │
                          │  └──────────────────────────────┘ │
                          └────────────────────────────────────┘
                                         │
                                         ▼
                          ┌────────────────────────────────────┐
                          │     DATABASE LAYER (database.py)   │
                          │                                    │
                          │  ┌──────────────────────────────┐ │
                          │  │    Crypto Manager            │ │
                          │  │  • AES-256 encryption        │ │
                          │  │  • RSA-2048 keys             │ │
                          │  │  • SHA-256 signatures        │ │
                          │  └──────────────────────────────┘ │
                          │              │                     │
                          │              ▼                     │
                          │  ┌──────────────────────────────┐ │
                          │  │   Database Operations        │ │
                          │  │  • add_phishing_report()     │ │
                          │  │  • decrypt_report()          │ │
                          │  │  • verify_user()             │ │
                          │  │  • log_access()              │ │
                          │  └──────────────────────────────┘ │
                          └────────────────────────────────────┘
                                         │
                                         ▼
                          ┌────────────────────────────────────┐
                          │   SQLite Database (phishing_database.db) │
                          │                                    │
                          │  • phishing_reports (encrypted)    │
                          │  • whitelist                       │
                          │  • access_log                      │
                          │  • users                           │
                          └────────────────────────────────────┘
```

## Authentication Flow

```
┌──────────┐                                         ┌──────────┐
│  User    │                                         │  Server  │
└─────┬────┘                                         └─────┬────┘
      │                                                    │
      │  1. POST /api/auth/login                          │
      │     { username, password }                        │
      ├──────────────────────────────────────────────────▶│
      │                                                    │
      │                             2. BCrypt verify       │
      │                                password            │
      │                                                    │
      │  3. JWT token + user data                         │
      │◀──────────────────────────────────────────────────┤
      │                                                    │
      │  4. Store token in localStorage                   │
      │  5. Redirect to /dashboard                        │
      │                                                    │
      │  6. GET /api/reports                              │
      │     Authorization: Bearer <token>                 │
      ├──────────────────────────────────────────────────▶│
      │                                                    │
      │                             7. Validate token      │
      │                                Decode username     │
      │                                Log access          │
      │                                                    │
      │  8. Encrypted reports data                        │
      │◀──────────────────────────────────────────────────┤
      │                                                    │
      │  9. Display in UI                                 │
      │                                                    │
```

## Report Decryption Flow

```
┌──────────┐                                         ┌──────────┐
│  Admin   │                                         │  Server  │
└─────┬────┘                                         └─────┬────┘
      │                                                    │
      │  1. Click "Decrypt" button                        │
      │     on Report ID: 42                              │
      │                                                    │
      │  2. POST /api/reports/42/decrypt                  │
      │     Authorization: Bearer <token>                 │
      ├──────────────────────────────────────────────────▶│
      │                                                    │
      │                        3. Validate JWT token       │
      │                        4. Fetch encrypted report   │
      │                        5. Load RSA private key     │
      │                        6. Decrypt AES key          │
      │                        7. Decrypt URL              │
      │                        8. Decrypt metadata         │
      │                        9. Verify signature         │
      │                       10. Log decrypt action       │
      │                                                    │
      │ 11. Decrypted data                                │
      │     { url, metadata, signature_valid }            │
      │◀──────────────────────────────────────────────────┤
      │                                                    │
      │ 12. Display decrypted information                 │
      │     • URL: http://phishing-site.com               │
      │     • Probability: 95%                            │
      │     • Source: ML Model                            │
      │     • VirusTotal: 5 reports                       │
      │     • Signature: ✓ Valid                          │
      │                                                    │
```

## Component Hierarchy

```
App.jsx (Root)
│
├─ AuthProvider (Context)
│
├─ BrowserRouter
│  │
│  ├─ Route: /login
│  │  └─ Login.jsx
│  │
│  └─ Route: / (Protected)
│     └─ Layout.jsx
│        │
│        ├─ Sidebar Navigation
│        │  ├─ Dashboard link
│        │  ├─ Reports link
│        │  ├─ Whitelist link
│        │  └─ Audit Logs link
│        │
│        ├─ Header
│        │  └─ Security Badge
│        │
│        └─ Outlet (Route content)
│           │
│           ├─ Route: /dashboard
│           │  └─ Dashboard.jsx
│           │     ├─ StatCard (x4)
│           │     ├─ AreaChart (Trends)
│           │     ├─ BarChart (Top Threats)
│           │     └─ Activity Feed
│           │
│           ├─ Route: /reports
│           │  └─ Reports.jsx
│           │     ├─ Search Bar
│           │     ├─ Report Card (list)
│           │     │  ├─ Encrypted view
│           │     │  └─ Decrypted view
│           │     └─ Export button
│           │
│           ├─ Route: /whitelist
│           │  └─ Whitelist.jsx
│           │     ├─ Search Bar
│           │     ├─ Stats Card
│           │     ├─ Domain Table
│           │     └─ Add Domain Modal
│           │
│           └─ Route: /audit
│              └─ AuditLogs.jsx
│                 ├─ Filter Dropdown
│                 ├─ Stats Cards (x3)
│                 ├─ Activity Timeline
│                 └─ Export button
```

## Data Flow: Adding to Whitelist

```
1. User clicks "Add Domain" button
   ↓
2. Modal opens with form
   ↓
3. User enters:
   • Domain: example.com
   • Reason: Testing whitelist
   ↓
4. User clicks "Add Domain"
   ↓
5. React: handleAdd() called
   ↓
6. Axios: POST /api/whitelist
   {
     domain: "example.com",
     reason: "Testing whitelist"
   }
   Headers: {
     Authorization: "Bearer <jwt_token>"
   }
   ↓
7. Flask: @token_required validates token
   ↓
8. Flask: Decodes JWT, gets username
   ↓
9. Database: add_to_whitelist()
   INSERT INTO whitelist (domain, added_by, reason)
   VALUES ('example.com', 'admin', 'Testing')
   ↓
10. Database: log_access()
    INSERT INTO access_log (username, action, details)
    VALUES ('admin', 'add_whitelist', 'Domain: example.com')
    ↓
11. Flask: Returns success response
    { message: "Domain added to whitelist" }
    ↓
12. React: Receives response
    ↓
13. React: Calls loadWhitelist() to refresh
    ↓
14. React: Modal closes
    ↓
15. UI: Table updates with new domain
```

## Security Layers

```
┌─────────────────────────────────────────────────────────┐
│                    Layer 5: UI Security                  │
│  • Input validation                                      │
│  • XSS prevention                                        │
│  • CSRF tokens (if needed)                               │
└─────────────────────────────────────────────────────────┘
                           ▲
                           │
┌─────────────────────────────────────────────────────────┐
│                 Layer 4: Transport Security              │
│  • JWT tokens (24-hour expiry)                           │
│  • Bearer token authentication                           │
│  • CORS configuration                                    │
└─────────────────────────────────────────────────────────┘
                           ▲
                           │
┌─────────────────────────────────────────────────────────┐
│              Layer 3: Application Security               │
│  • @token_required decorator                             │
│  • BCrypt password hashing (12 rounds)                   │
│  • User verification                                     │
│  • Audit logging                                         │
└─────────────────────────────────────────────────────────┘
                           ▲
                           │
┌─────────────────────────────────────────────────────────┐
│                Layer 2: Encryption Layer                 │
│  • AES-256-GCM (symmetric encryption)                    │
│  • RSA-2048 (asymmetric encryption)                      │
│  • SHA-256 (digital signatures)                          │
└─────────────────────────────────────────────────────────┘
                           ▲
                           │
┌─────────────────────────────────────────────────────────┐
│                 Layer 1: Data Security                   │
│  • Encrypted database storage                            │
│  • Blockchain hash linking                               │
│  • Tamper detection                                      │
└─────────────────────────────────────────────────────────┘
```

## Technology Stack

```
┌─────────────────────────────────────────────────────────┐
│                       FRONTEND                           │
├─────────────────────────────────────────────────────────┤
│  React 18              │  UI Library                     │
│  React Router 6        │  Navigation                     │
│  Tailwind CSS 3        │  Styling                        │
│  Axios 1.6             │  HTTP Client                    │
│  Recharts 2.10         │  Charts                         │
│  Lucide React          │  Icons                          │
│  Vite 5                │  Build Tool                     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                       BACKEND                            │
├─────────────────────────────────────────────────────────┤
│  Flask 3.x             │  Web Framework                  │
│  Flask-CORS            │  Cross-Origin                   │
│  PyJWT 2.x             │  Token Generation               │
│  BCrypt                │  Password Hashing               │
│  Cryptography          │  Encryption                     │
│  SQLite 3              │  Database                       │
│  Scikit-learn          │  ML Model                       │
│  Pandas                │  Data Processing                │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                       SECURITY                           │
├─────────────────────────────────────────────────────────┤
│  AES-256-GCM           │  Symmetric Encryption           │
│  RSA-2048              │  Asymmetric Encryption          │
│  SHA-256               │  Hashing & Signatures           │
│  BCrypt                │  Password Hashing               │
│  JWT                   │  Authentication Tokens          │
└─────────────────────────────────────────────────────────┘
```

## File Structure with Sizes

```
admin_panel/
├── src/                           (~2000 lines)
│   ├── components/                (~400 lines)
│   │   ├── Layout.jsx            (150 lines)
│   │   └── ProtectedRoute.jsx    (50 lines)
│   ├── context/                   (~100 lines)
│   │   └── AuthContext.jsx       (100 lines)
│   ├── pages/                     (~1200 lines)
│   │   ├── Login.jsx             (170 lines)
│   │   ├── Dashboard.jsx         (250 lines)
│   │   ├── Reports.jsx           (350 lines)
│   │   ├── Whitelist.jsx         (250 lines)
│   │   └── AuditLogs.jsx         (180 lines)
│   ├── utils/                     (~150 lines)
│   │   └── api.js                (150 lines)
│   ├── App.jsx                    (40 lines)
│   ├── main.jsx                   (10 lines)
│   └── index.css                  (40 lines)
├── package.json                   (35 lines)
├── vite.config.js                 (15 lines)
├── tailwind.config.js             (30 lines)
└── README.md                      (350 lines)

server/
├── app.py                         (600 lines) - MODIFIED
├── database.py                    (650 lines) - MODIFIED
├── crypto_utils.py                (500 lines) - Phase 1-2
├── setup_admin.py                 (60 lines)  - NEW
└── test_system.py                 (150 lines) - Phase 1-2

documentation/
├── QUICK_START_ADMIN.md           (300 lines)
├── PHASE3_COMPLETE.md             (800 lines)
└── IMPLEMENTATION_SUMMARY.md      (500 lines)

Total Lines of Code: ~5,000+
Total Files Created: 25+
```

---

**Architecture designed for security, scalability, and maintainability.**
